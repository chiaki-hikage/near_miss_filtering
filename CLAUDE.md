## プロジェクト概要

自動運転データセットから、ヒヤリハット候補となる走行区間を効率的に抽出するためのプロジェクト。

CAN、IMU、GNSSなどのセンサ時系列から、急ブレーキ、急操舵、急車線変更、蛇行などの特徴を検出し、大量の走行データから確認対象を絞り込む。

## 主な目的

* ヒヤリハット候補を抽出する特徴量・条件の検討
* CAN/IMU等のセンサデータからイベント候補を抽出
* 欠損、ノイズ、サンプリング周期の違いを考慮したロバスト性評価
* 将来的な自動運転学習・評価データ取得の効率化

## 対象イベント

* 急ブレーキ
* 急加速
* 急操舵
* 急車線変更
* 蛇行
* その他の異常・危険挙動候補

## 主な特徴量候補

* 車速
* 縦加速度・横加速度
* jerk
* steering angle / steering rate
* yaw rate
* wheel speed
* センサ値の変化率・反転回数

## データ

主に実走行のCAN・IMU等の時系列データを対象とする。

初期検証では `comma2k19` などの公開データセットを利用する。映像は原則として特徴抽出には使用せず、必要に応じて抽出結果の確認用途として扱う。

## 実装方針

* まず単純で解釈可能な閾値・ルールベース手法を優先する
* 生データと加工済みデータを明確に区別する
* 欠損値や外れ値を安易に補完せず、元データの品質を確認する
* 特徴量・閾値・判定根拠が追跡できる実装にする
* データセット固有の処理と汎用的なイベント検出処理を分離する

## コーディング上の注意

* Pythonを使用する
* 処理は可能な限り関数化する
* パラメータや閾値をハードコードせず設定として管理する
* 元データを上書きしない
* 中間結果と抽出理由を確認できるようにする
* 不明な信号の意味や単位を推測で決めない
* Pythonのパッケージ・仮想環境管理には `uv` を使用する
* 依存関係は `pyproject.toml` で管理する
* パッケージの追加・削除は原則として `uv add` / `uv remove` を使用する
* Pythonスクリプトは原則 `uv run` 経由で実行する
